# Neon PostgreSQL — use your development branch URL
DATABASE_URL="postgresql://user:password@your-neon-host.neon.tech/dbname?sslmode=require"

# Email verification — set to "true" to require email verification on registration
# Requires a verified domain in Resend. When "false" or unset, users are auto-verified.
ENABLE_EMAIL_VERIFICATION="false"
